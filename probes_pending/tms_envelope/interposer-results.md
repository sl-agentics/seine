# R1 interposer ladder — RESULTS (read against interposer-predictions.md)

_2026-07-12, run immediately after the v2 0-div gate closed
(1800/1800). `interposer_ladder.py --run`: oracle 3× per cell, all
six 3×-stable, **6/6 GREEN — every prediction (mechanical model_sd
output, logged pre-run) exact.** Zero adjustments, zero flakes._

## What each cell confirmed

- **ip_a1/a2 (min812-spine, lazy k0)**: the lazy drop lands at the
  justifier's POP and the transient window is open to EVERY
  strictly-higher rule — the @10 observer AND the @5 interposer fire
  (salience order); sub-salience (@-5) never does. The window is a
  salience THRESHOLD, not a top-of-queue privilege.
- **ip_a3 (eager k0)**: the eager run-end landing admits NO
  ≤-salience glimpse at all — the interposer stayed silent. The
  D-187 eager row transfers to k0-with-interposer unchanged.
- **ip_b1/b2 (9133-spine, lazy k1 fan-out ×3)**: clause B
  (in-firing self-cancellation) governs the fan-out spine — the
  justifier fires gen-1 only and STARVES (no deleter = no touch =
  no revive; the D-196 starvation law's no-deleter face); both
  higher rules glimpse gen-1's LK exactly once.
- **ip_c1 (the D-195 (b) between-row)**: the zombie-justifier
  window is open to every rule strictly between the justifier and
  the top — RO2@7 fires twice, THEN the @6 interposer fires once on
  LK2(2), then RJ's pop kills it. Completes the row gt14 opened
  (below ⇒ never) with between ⇒ yes, in salience order.

## The R1 deliverable state

The (cloud × belief-loss) landing rows now form a CONFIRMED,
population-certified table — the D-187/D-188 three-clause L-SD row +
the D-195 eval-consumption landing (update-break and del lanes, the
RHS-order race, the mutfirst/ilfirst × lead/trail matrix) + the
D-196 two-phase unbreak/starvation and fold laws — all encoded in
model_sd.py, 0-div on 1800 fresh-grammar cases across 12 seeds, and
now interposer-verified at the landing-time level on both named
spines. The fifth-law-shaped hole has a table; the ⚠ D-106 region
was never entered (probe-only rung; engine untouched).

NEXT (the ladder's later rungs, Bryan's call): the ENGINE A/B on
these cells + the envelope's 13 L-SD / 18 L-MB xfail witnesses
re-read against the completed table (which witnesses the table now
fully explains — the port target list); then the L-MB boundary
ladder (plan §5.2) or the port slab. I-RD stays LAST per Bryan's
sequencing.
