{
  "name": "sn_g5",
  "types": [
    {"name": "A", "fields": [{"name": "k", "type": "i64"}]},
    {"name": "B", "fields": [{"name": "m", "type": "i64"}]},
    {"name": "C", "fields": [{"name": "k", "type": "i64"}]},
    {"name": "D", "fields": [{"name": "m", "type": "i64"}]}
  ],
  "facts": [
    {"type": "A", "fields": {"k": 1}},
    {"type": "B", "fields": {"m": 1}},
    {"type": "C", "fields": {"k": 1}}
  ],
  "drl": "rule R1 when not(A() and exists(B())) then end\nrule R2 when not(C() and exists(D())) then end\nrule R3 when not(A() and not(B())) then end\nrule R4 when not(C() and not(D())) then end\n"
}
