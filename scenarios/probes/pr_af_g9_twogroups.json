{
 "drl": "rule L1 salience -5 when $s : S($k : k) then drools.setFocus(\"g\"); insert(new X($k)); end\nrule L2 salience -6 when $t : T($k : k) then drools.setFocus(\"g2\"); insert(new X($k)); end\nrule H salience 5 when X($v : v) then end\nrule GD agenda-group \"g\" when X($v : v) not S(k == 1) then end\nrule GD2 agenda-group \"g2\" when X($v : v) not S(k == 1) then end\n",
 "types": [
  {
   "name": "S",
   "fields": [
    {
     "name": "k",
     "type": "i64"
    }
   ]
  },
  {
   "name": "T",
   "fields": [
    {
     "name": "k",
     "type": "i64"
    }
   ]
  },
  {
   "name": "X",
   "fields": [
    {
     "name": "v",
     "type": "i64"
    }
   ]
  }
 ],
 "facts": [
  {
   "type": "S",
   "fields": {
    "k": 1
   }
  },
  {
   "type": "S",
   "fields": {
    "k": 2
   }
  },
  {
   "type": "T",
   "fields": {
    "k": 11
   }
  },
  {
   "type": "T",
   "fields": {
    "k": 12
   }
  }
 ],
 "epochs": [],
 "name": "pr_af_g9_twogroups"
}
