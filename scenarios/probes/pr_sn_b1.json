{
  "name": "sn_b1",
  "types": [
    {"name": "G", "fields": [{"name": "g", "type": "i64"}]},
    {"name": "P", "fields": [{"name": "f0", "type": "i64"}]},
    {"name": "A", "fields": [{"name": "k", "type": "i64"}]},
    {"name": "B", "fields": [{"name": "m", "type": "i64"}]}
  ],
  "facts": [
    {"type": "G", "fields": {"g": 1}},
    {"type": "P", "fields": {"f0": 1}},
    {"type": "A", "fields": {"k": 1}}
  ],
  "drl": "rule R_hi salience 10 when $g : G() then insert(new B(1)); end\nrule R_not when $p : P() not(A() and B()) then end\nrule R_ex when $p : P() exists(A() and B()) then end\n"
}
