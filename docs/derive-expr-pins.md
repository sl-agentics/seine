# Derive expression-layer semantics pins (oracle: duckdb 1.5.4)

Generated by tools/pin_derive_expr.py — the measured ground truth
behind seine_rs.derive's expression layer (with_columns / filter)
and its pure-python reference evaluator. Regenerate and diff on any
oracle version bump. All BIGINT literals are cast explicitly: bare
integer literals in DuckDB are INT32 and change overflow behavior.

**The decision rule** (on the record): measured DuckDB behavior wins
on VALUE semantics (rem signs, rounding direction, Kleene tables,
CASE-null); the house loud-error doctrine wins on ERROR policy
(integer overflow, division by zero, failed casts are loud errors in
the kernels, never silent null manufacture — where DuckDB errors too,
the fuzzer treats both as matching error outcomes); IEEE-754 wins on
float specials (NaN/inf are VALUES; x/0.0 is ±inf; NaN != NaN). Every
deliberate divergence from the oracle lives in the ledger at the
bottom of this file.

## A. Integer division, floor division, remainder (signs)

| expression | result |
|---|---|
| `CAST(7 AS BIGINT) / CAST(2 AS BIGINT)` | 3.5 |
| `CAST(-7 AS BIGINT) / CAST(2 AS BIGINT)` | -3.5 |
| `typeof(CAST(7 AS BIGINT) / CAST(2 AS BIGINT))` | 'DOUBLE' |
| `CAST(7 AS BIGINT) // CAST(2 AS BIGINT)` | 3 |
| `CAST(-7 AS BIGINT) // CAST(2 AS BIGINT)` | -3 |
| `CAST(7 AS BIGINT) // CAST(-2 AS BIGINT)` | -3 |
| `CAST(-7 AS BIGINT) // CAST(-2 AS BIGINT)` | 3 |
| `typeof(CAST(7 AS BIGINT) // CAST(2 AS BIGINT))` | 'BIGINT' |
| `CAST(7 AS BIGINT) % CAST(3 AS BIGINT)` | 1 |
| `CAST(-7 AS BIGINT) % CAST(3 AS BIGINT)` | -1 |
| `CAST(7 AS BIGINT) % CAST(-3 AS BIGINT)` | 1 |
| `CAST(-7 AS BIGINT) % CAST(-3 AS BIGINT)` | -1 |
| `CAST(7.5 AS DOUBLE) % CAST(3.0 AS DOUBLE)` | 1.5 |
| `CAST(-7.5 AS DOUBLE) % CAST(3.0 AS DOUBLE)` | -1.5 |
| `CAST(7.5 AS DOUBLE) // CAST(2.0 AS DOUBLE)` | 3.75 |
| `CAST(-7.5 AS DOUBLE) // CAST(2.0 AS DOUBLE)` | -3.75 |
| `typeof(CAST(7.5 AS DOUBLE) // CAST(2.0 AS DOUBLE))` | 'DOUBLE' |

## B. Division / remainder by zero

| expression | result |
|---|---|
| `CAST(1 AS BIGINT) / CAST(0 AS BIGINT)` | inf |
| `CAST(1 AS BIGINT) // CAST(0 AS BIGINT)` | NULL |
| `CAST(1 AS BIGINT) % CAST(0 AS BIGINT)` | NULL |
| `CAST(1.0 AS DOUBLE) / CAST(0.0 AS DOUBLE)` | inf |
| `CAST(-1.0 AS DOUBLE) / CAST(0.0 AS DOUBLE)` | -inf |
| `CAST(0.0 AS DOUBLE) / CAST(0.0 AS DOUBLE)` | nan |
| `CAST(1.0 AS DOUBLE) % CAST(0.0 AS DOUBLE)` | nan |

## C. BIGINT overflow edges

| expression | result |
|---|---|
| `CAST(9223372036854775807 AS BIGINT) + CAST(1 AS BIGINT)` | ERROR: Out of Range Error: Overflow in addition of INT64 (9223372036854775807 + 1)! |
| `CAST(9223372036854775807 AS BIGINT) * CAST(2 AS BIGINT)` | ERROR: Out of Range Error: Overflow in multiplication of INT64 (9223372036854775807 * 2)! |
| `CAST(-9223372036854775808 AS BIGINT) - CAST(1 AS BIGINT)` | ERROR: Out of Range Error: Overflow in subtraction of INT64 (-9223372036854775808 - 1)! |
| `-(CAST(-9223372036854775808 AS BIGINT))` | ERROR: Out of Range Error: Overflow in negation of numeric value! |
| `CAST(-9223372036854775808 AS BIGINT) // CAST(-1 AS BIGINT)` | ERROR: Out of Range Error: Overflow in division of -9223372036854775808 / -1 |
| `CAST(-9223372036854775808 AS BIGINT) % CAST(-1 AS BIGINT)` | ERROR: Out of Range Error: Overflow in division of -9223372036854775808 / -1 |
| `abs(CAST(-9223372036854775808 AS BIGINT))` | ERROR: Out of Range Error: Overflow on abs(-9223372036854775808) |

## D. DOUBLE -> BIGINT casts (rounding direction, overflow, specials)

| expression | result |
|---|---|
| `CAST(CAST(2.5 AS DOUBLE) AS BIGINT)` | 2 |
| `CAST(CAST(3.5 AS DOUBLE) AS BIGINT)` | 4 |
| `CAST(CAST(-2.5 AS DOUBLE) AS BIGINT)` | -2 |
| `CAST(CAST(2.4 AS DOUBLE) AS BIGINT)` | 2 |
| `CAST(CAST(2.6 AS DOUBLE) AS BIGINT)` | 3 |
| `CAST(CAST(-2.6 AS DOUBLE) AS BIGINT)` | -3 |
| `CAST(CAST(1e300 AS DOUBLE) AS BIGINT)` | ERROR: Conversion Error: Type DOUBLE with value 1e+300 can't be cast because the value is out of range for the destination type INT64 |
| `CAST(CAST('NaN' AS DOUBLE) AS BIGINT)` | ERROR: Conversion Error: Type DOUBLE with value nan can't be cast because the value is out of range for the destination type INT64 |
| `CAST(CAST('Infinity' AS DOUBLE) AS BIGINT)` | ERROR: Conversion Error: Type DOUBLE with value inf can't be cast because the value is out of range for the destination type INT64 |
| `CAST(CAST(5 AS BIGINT) AS DOUBLE)` | 5.0 |

## E. round() (half cases, ndigits, negative ndigits)

| expression | result |
|---|---|
| `round(CAST(2.5 AS DOUBLE))` | 3.0 |
| `round(CAST(3.5 AS DOUBLE))` | 4.0 |
| `round(CAST(-2.5 AS DOUBLE))` | -3.0 |
| `round(CAST(0.5 AS DOUBLE))` | 1.0 |
| `round(CAST(2.345 AS DOUBLE), 2)` | 2.35 |
| `round(CAST(2.675 AS DOUBLE), 2)` | 2.68 |
| `round(CAST(1234.5 AS DOUBLE), -2)` | 1200.0 |
| `round(CAST(1250.0 AS DOUBLE), -2)` | 1300.0 |
| `typeof(round(CAST(2.5 AS DOUBLE)))` | 'DOUBLE' |
| `round(CAST(7 AS BIGINT))` | 7 |
| `round(CAST(2.665 AS DOUBLE), 2)` | 2.67 |
| `round(CAST(2.685 AS DOUBLE), 2)` | 2.69 |
| `round(CAST(0.125 AS DOUBLE), 2)` | 0.13 |
| `round(CAST(0.135 AS DOUBLE), 2)` | 0.14 |
| `round(CAST(-2.675 AS DOUBLE), 2)` | -2.68 |

## F. floor / ceil / sqrt / pow

| expression | result |
|---|---|
| `floor(CAST(2.5 AS DOUBLE))` | 2.0 |
| `floor(CAST(-2.5 AS DOUBLE))` | -3.0 |
| `ceil(CAST(2.5 AS DOUBLE))` | 3.0 |
| `ceil(CAST(-2.5 AS DOUBLE))` | -2.0 |
| `typeof(floor(CAST(2.5 AS DOUBLE)))` | 'DOUBLE' |
| `sqrt(CAST(-1.0 AS DOUBLE))` | ERROR: Out of Range Error: cannot take square root of a negative number |
| `sqrt(CAST(0.0 AS DOUBLE))` | 0.0 |
| `pow(CAST(2 AS BIGINT), CAST(10 AS BIGINT))` | 1024.0 |
| `typeof(pow(CAST(2 AS BIGINT), CAST(10 AS BIGINT)))` | 'DOUBLE' |
| `pow(CAST(2.0 AS DOUBLE), CAST(-1.0 AS DOUBLE))` | 0.5 |
| `pow(CAST(0.0 AS DOUBLE), CAST(0.0 AS DOUBLE))` | 1.0 |

## G. CASE with a NULL condition (if_else semantics)

| expression | result |
|---|---|
| `CASE WHEN CAST(NULL AS BOOLEAN) THEN 'then' ELSE 'else' END` | 'else' |
| `CASE WHEN TRUE THEN 'then' ELSE 'else' END` | 'then' |
| `CASE WHEN FALSE THEN 'then' ELSE 'else' END` | 'else' |

## H. String ops x NULL

| expression | result |
|---|---|
| `'a' || CAST(NULL AS VARCHAR)` | NULL |
| `concat('a', CAST(NULL AS VARCHAR))` | 'a' |
| `contains(CAST(NULL AS VARCHAR), 'a')` | NULL |
| `contains('abc', CAST(NULL AS VARCHAR))` | NULL |
| `starts_with(CAST(NULL AS VARCHAR), 'a')` | NULL |
| `ends_with('abc', CAST(NULL AS VARCHAR))` | NULL |
| `length(CAST(NULL AS VARCHAR))` | NULL |
| `typeof(length('abc'))` | 'BIGINT' |
| `'b' < 'a'` | False |
| `'B' < 'a'` | True |

## I. Comparisons x NULL (the derive registry's six)

| expression | result |
|---|---|
| `CAST(NULL AS BIGINT) = 1` | NULL |
| `CAST(NULL AS BIGINT) <> 1` | NULL |
| `CAST(NULL AS BIGINT) < 1` | NULL |
| `CAST(NULL AS BIGINT) <= 1` | NULL |
| `CAST(NULL AS BIGINT) > 1` | NULL |
| `CAST(NULL AS BIGINT) >= 1` | NULL |
| `CAST(NULL AS BIGINT) = CAST(NULL AS BIGINT)` | NULL |

## J. Kleene connectives (locality re-emit; full tables in duckdb-datatype-pins.md)

| a | b | a AND b | a OR b |
|---|---|---|---|
| TRUE | TRUE | True | True |
| TRUE | FALSE | False | True |
| TRUE | NULL | NULL | True |
| FALSE | TRUE | False | True |
| FALSE | FALSE | False | False |
| FALSE | NULL | False | NULL |
| NULL | TRUE | NULL | True |
| NULL | FALSE | False | NULL |
| NULL | NULL | NULL | NULL |

## K. Float specials as DuckDB sees them (ledger inputs)

| expression | result |
|---|---|
| `CAST('NaN' AS DOUBLE) = CAST('NaN' AS DOUBLE)` | True |
| `CAST('NaN' AS DOUBLE) < CAST('Infinity' AS DOUBLE)` | False |
| `CAST('Infinity' AS DOUBLE) + CAST(1.0 AS DOUBLE)` | inf |
| `CAST('NaN' AS DOUBLE) + CAST(1.0 AS DOUBLE)` | nan |

## L. WHERE with a NULL predicate (filter() semantics)

- `t_w(v) = [1, NULL, 5]`; `WHERE v > 2` -> rows: [5]
  (NULL predicate rows DROP — derive.filter matches this.)

## The divergence ledger (deliberate, on the record)

| # | surface | oracle (measured above) | the kernels | why |
|---|---|---|---|---|
| 1 | float NaN comparisons | NaN = NaN is TRUE (value/total order) | IEEE: NaN != NaN | floats follow IEEE-754 end to end; DuckDB's NaN-as-orderable-value is a SQL convention the dataframe world does not share. The fuzzer's IEEE side-battery excludes DuckDB. |
| 2 | `%` vs Python | sign of DIVIDEND (C semantics) | same as oracle | divergence is from PYTHON'S floored `%`, documented in the docstring; the oracle and kernels agree. |
| 3 | `round` vs Python | half AWAY FROM ZERO | same as oracle | divergence is from Python's banker's rounding, documented in the docstring; oracle and kernels agree (and match the shipped haversine kernel). |
| 4 | error policy | (see B/C: what errors vs what yields NULL) | int overflow / div0 / failed cast are ALWAYS loud errors | the house doctrine: silent null manufacture is corruption; where the oracle errors too the fuzzer matches error-to-error, where it yields a value the pin above is the spec. |
| 5 | string concat builtin | `concat()` skips NULLs | kernels use `\|\|` semantics (null propagates) | the battery's SQL translation uses `\|\|`, never `concat()`. |
| 6 | `//` `%` by zero (int) | NULL (section B) | loud error | ledger row 4's policy applied; the battery maps oracle-NULL-on-zero-divisor to the kernels' error for these two ops. |
| 7 | `//` on DOUBLE | plain division (7.5 // 2.0 = 3.75, section A) | `//` is INTEGER-ONLY — a float operand is a loud type error steering to `/` + `.floor()` | DuckDB's float `//` quirk answers no user question; restricting the operator keeps it honest. On the integer domain kernels and oracle agree (truncation; C-family div/mod identity with `%`). Python's FLOORED `//` divergence is documented in the docstring. |
| 8 | `round(x, n)` algorithm | rounds the SHORTEST-DECIMAL representation half-away (2.675 -> 2.68 though the binary value is 2.67499...; all fp-tie forensics in section E consistent) | same, by construction | kernels + pure-python reference both implement shortest-roundtrip-decimal rounding (Rust fmt / Python repr are both shortest-roundtrip); agrees with f64::round half-away at n=0, so the shipped haversine kernel's rounding is unchanged. |
| 9 | `if_else`/`fill_null` branch evaluation | SQL CASE is LAZY (an error in the untaken branch never surfaces) | EAGER — both branches evaluate vectorized over every row; a row-level error (div0, overflow) in either branch errors the batch even where that branch is not selected | dataframe-engine reality (polars behaves the same); the battery accepts oracle-ok/kernel-div0-error for exactly this class. Guard divisors before dividing. |
| 10 | float comparison semantics | NaN equals itself and sorts LAST (NaN = NaN TRUE, NaN < inf FALSE — section K); ±0 equal | STANDARD IEEE via native f64 operators: NaN != everything including itself; -0.0 == 0.0 | the arrow cmp kernels use totalOrder (-0.0 != 0.0, NaN == NaN) and are NOT used for f64 — the kernels hand-roll IEEE, matching the oracle at ±0 and the published contract at NaN. The battery's IEEE side-battery (NaN/inf column data) excludes the oracle. |
| 11 | `i64::MIN % -1` | error (section C: overflow in division) | 0 (arrow's rem; mathematically exact — the overflow is an artifact of computing the quotient) | the fuzz pools carry no i64::MIN so the class is pinned by vector, not fuzzed. |

