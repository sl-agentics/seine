[workspace]
resolver = "2"
members = ["engine", "harness", "bindings"]
# bindings (pyo3) build only on demand: `cargo build -p seine-py` / maturin
default-members = ["engine", "harness"]

[workspace.package]
version = "0.4.2"
edition = "2021"
license = "Apache-2.0"
repository = ""
